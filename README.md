--------
# Module 1
1. Create venv using "conda create -p venv python=3.10"
2. to activate it: conda activate venv/
3. to deactivate it: conda deactivate
4. to install packages: pip install -r requirements.txt 
5. to run ipynb: pip install ipykernel
--------
